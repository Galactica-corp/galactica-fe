export const CARDS_MAP: {
  [key: string]: {
    name: string;
    kycTitle: string;
    background?: string;
    logo?: string;
  };
} = {
  accreditation: {
    name: "Accreditation Certificate",
    kycTitle: "Accreditation Certificate",
  },
};
